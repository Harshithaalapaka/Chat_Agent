A simple LLM-powered chat agent built using Groq LLM, FastAPI, SQLite, and a basic HTML/CSS/JS frontend.
The system stores conversation history in SQLite so the assistant can remember previous messages.